"""Song Story module — unified interpretation layer between analyzer and generator.

This module transforms a HierarchyResult into a structured song story JSON that
provides rich per-section data (roles, energy, stems, lighting guidance) and
dramatic moments for downstream sequence generation.
"""
