# Scoring Test Fixtures

Fixture TimingTrack definitions with known properties for deterministic scoring tests.
These are defined in Python in `tests/fixtures/scoring/tracks.py` rather than JSON,
so they can be imported directly by test modules.
